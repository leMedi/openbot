{
  "name": "openbot",
  "private": true,
  "packageManager": "pnpm@10.18.1",
  "scripts": {
    "with-env": "dotenv -e .env --",
    "dev": "turbo dev",
    "build": "turbo build",
    "build:server-runtime": "esbuild scripts/run-server.mjs --bundle --platform=node --format=esm --target=node24 --alias:@openbot/db=./packages/db/src/index.ts --external:../dist/server/server.js --banner:js=\"import { createRequire } from 'node:module'; const require = createRequire(import.meta.url);\"",
    "typecheck": "turbo typecheck",
    "clean": "pnpm --filter @openbot/tools clean",
    "db:generate": "turbo db:generate --filter=@openbot/db",
    "db:migrate": "turbo db:migrate --filter=@openbot/db",
    "db:studio": "turbo db:studio --filter=@openbot/db"
  },
  "devDependencies": {
    "dotenv-cli": "11.0.0",
    "esbuild": "0.28.2",
    "turbo": "2.10.12"
  }
}
